import { TrackHeightDirective } from './track-height.directive';

describe('TrackHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackHeightDirective();
    expect(directive).toBeTruthy();
  });
});
